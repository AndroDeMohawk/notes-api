services:
  postgres:
    container_name: pg_db
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: 1010
    volumes:
      - ./postgres-data:/var/lib/postgres/data
    ports:
      - "1111:5432"